services:
  vaultwarden:
    image: vaultwarden/server:1.37.2-alpine
    container_name: vaultwarden
    restart: always
    environment:
      SIGNUPS_ALLOWED: "false"
    volumes:
      - ./data:/data
